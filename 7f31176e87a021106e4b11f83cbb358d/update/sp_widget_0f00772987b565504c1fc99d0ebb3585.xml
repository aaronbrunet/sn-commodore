<?xml version="1.0" encoding="UTF-8"?><record_update table="sp_widget">
    <sp_widget action="INSERT_OR_UPDATE">
        <category>other</category>
        <client_script><![CDATA[function client($scope, spUIActionsExecuter, spUtil, spModal) {
	var c = this;
	
	var ESIGNATURE = {
		"approved": "cbfe291147220100ba13a5554ee4904d",
		"rejected": "580f711147220100ba13a5554ee4904b"
	};
	
	spUtil.recordWatch($scope, "sysapproval_approver", "state=requested^sys_id="+ c.data.sys_id);
	c.approvalInProgress = false;
	c.action = function(state) {
		if(c.data.esignature.e_sig_required) {
			var requestParams = {
				username: c.data.esignature.username,
				userSysId: c.data.esignature.userSysId
			};
			spUIActionsExecuter.executeFormAction(ESIGNATURE[state], "sysapproval_approver" , c.data.sys_id, [] , "", requestParams).then(function(response) {
				});
		} else {
			c.approvalInProgress = true;
				c.data.op = state;
				c.server.update().then(function() {
					c.approvalInProgress = false;
					if (!c.data.updateID) // update failed
						spUtil.addErrorMessage(c.data.actionPreventedMsg);
					else {
						c.data.state = state;
						if (state === 'approved')
							spUtil.addInfoMessage(c.data.approvedStatusMsg);
					}
				});
		}
	}

	c.exceptionAction = function(state){
		if(c.data.exception_record){
			var msg = c.data.exception_record.msg + '<br/>Are you <b>sure</b> you want to do this?';
			spModal.open({
				title: 'Approve Exception Request',
				message: msg,
				buttons: [
					{label: 'Cancel', cancel: true},					
					{label: 'Approve exception', primary: true, class:'cc-button-warning'}
					]
			}).then(function(){
				c.action(state);
			});
		}

	};	

	$rootScope.$on('approval_clear', function(event,parms) {
		console.log('Receiving approval_clear: '+parms);
		$scope.data.all_clear = parms;
	});

	$rootScope.$on('exception_record', function(event,parms) {
		console.log('Receiving exception_record: '+JSON.stringify(parms));
		$scope.data.exception_record = parms;
	});
}]]></client_script>
        <controller_as>c</controller_as>
        <css>.question {&#13;
  text-align: center;&#13;
  margin-top: 1em;&#13;
  display: flex;&#13;
  flex-wrap: wrap;&#13;
}&#13;
&#13;
.spacer {&#13;
  display:inline-block;&#13;
  width:5%;&#13;
}&#13;
&#13;
.btn-question {&#13;
  flex-grow: 1;&#13;
  width: -webkit-fill-available;&#13;
  margin: 0.6rem 0;&#13;
}&#13;
&#13;
&#13;
.field-label,.field-span {&#13;
	display: inline-block;&#13;
}&#13;
.field-label {&#13;
	font-weight: bold;&#13;
}&#13;
</css>
        <data_table>sp_instance</data_table>
        <demo_data>{
  "data": {
    "fields": [
      {
        "type": "glide_date_time",
        "value": "2016-01-31T17:33:54+00:00",
        "label": "Approval Example Label"
      }
    ],
    "state": "requested",
    "label": "incident"
  },
  "options": {
    "color": "default"
  }
}</demo_data>
        <description/>
        <docs/>
        <field_list>color</field_list>
        <has_preview>true</has_preview>
        <id>cc-approval-info</id>
        <internal>false</internal>
        <link><![CDATA[function link(scope, element, attrs, controller) {  }]]></link>
        <name>CC Approval Info</name>
        <option_schema/>
        <public>false</public>
        <roles/>
        <script><![CDATA[(function() {
	data.actionPreventedMsg = gs.getMessage("Update failed");
	var gr = $sp.getRecord();
	if (gr == null || !gr.isValid()) {
		data.isValid = false;
		return;
	}
	data.all_clear = true;
	data.isValid = true;
	data.isMine = new x_snc_commodore_0.CCApprovalUtils().isApprovalMine(gr);
	var userApprovalAccess = gs.hasRole("approval_admin") || (gs.hasRole("approver_user") && data.isMine);
	
	var approverDisplay = gr.approver.getDisplayValue();
	if (!data.isMine && approverDisplay)
		data.approverDisplay = approverDisplay;
	
	if (approverDisplay) {
		data.approvedMsg = gs.getMessage("Approved by {0}", approverDisplay);
		data.rejectedMsg = gs.getMessage("Rejected by {0}", approverDisplay);
	} else {
		data.approvedMsg = gs.getMessage("Approved");
		data.rejectedMsg = gs.getMessage("Rejected");
	}
	if (input && input.op && userApprovalAccess) {
		gr.state = input.op;
		data.updateID = gr.update();
		if (!data.updateID || data.updateID == '') {
			// update failed so fetch again for correct values
			gr = $sp.getRecord();
		}
		data.op = "";
	}

	var fields = $sp.getFields(gr, 'state,sys_created_on');

	if (gr.sys_mod_count > 0)
		fields.push($sp.getField(gr, 'sys_updated_on'));

	data.fields = fields;
	data.state = gr.state.toString();
	data.sys_updated_on = gr.sys_updated_on.toString();
	data.sys_id = gr.getUniqueValue();
	data.table = gr.getTableName();
	var record = getRecordBeingApproved(gr);
	data.label = record.getLabel();
	data.approvedStatusMsg = gs.getMessage("{0} has been approved", record.number);
	data.esignature = {
		username:  gs.getUserName(),
		userSysId: gs.getUserID(),
		e_sig_required: checkESig(gr)
};
	function checkESig(approvalGR) {
		var esigRegistryGR = new GlideRecord("e_signature_registry");
		if (!esigRegistryGR.isValid())
			return false;
		
		var table = approvalGR.getValue("source_table");
		if (!table)
			table = approvalGR.sysapproval.sys_class_name;
		if (!table)
			return false;
		
		esigRegistryGR.addQuery("enabled", "true");
		esigRegistryGR.addQuery("table_name", table);
		esigRegistryGR.query();
		return esigRegistryGR.hasNext();
	}
	
	function getRecordBeingApproved(gr) {
		if (!gr.sysapproval.nil())
			return gr.sysapproval.getRefRecord();

		return gr.document_id.getRefRecord();
	}
})();
]]></script>
        <servicenow>false</servicenow>
        <sys_class_name>sp_widget</sys_class_name>
        <sys_created_by>aaron.brunet</sys_created_by>
        <sys_created_on>2023-03-25 21:14:55</sys_created_on>
        <sys_id>0f00772987b565504c1fc99d0ebb3585</sys_id>
        <sys_mod_count>25</sys_mod_count>
        <sys_name>CC Approval Info</sys_name>
        <sys_package display_value="Commodore's Club" source="x_snc_commodore_0">7f31176e87a021106e4b11f83cbb358d</sys_package>
        <sys_policy/>
        <sys_scope display_value="Commodore's Club">7f31176e87a021106e4b11f83cbb358d</sys_scope>
        <sys_update_name>sp_widget_0f00772987b565504c1fc99d0ebb3585</sys_update_name>
        <sys_updated_by>aaron.brunet</sys_updated_by>
        <sys_updated_on>2023-03-27 13:45:58</sys_updated_on>
        <template><![CDATA[<div ng-if="c.data.isValid" class="panel panel-{{::c.options.color}} b">  
  <div class="panel-heading">
    <h2 class="panel-title" ng-if="c.data.isMine && (c.data.state == 'requested')">${This request requires your approval}</h2>
    <h2 class="panel-title" ng-if="!c.data.isMine && (c.data.state == 'requested')">${This {{c.data.label}} requires approval <span ng-if="c.data.approverDisplay"> by {{c.data.approverDisplay}}}</span></h2>
    <h2 class="panel-title" ng-if="c.data.state == 'approved'">{{data.approvedMsg}}</h2>
    <h2 class="panel-title" ng-if="c.data.state == 'rejected'">{{data.rejectedMsg}}</h2>
  </div>  
  <div class="panel-body">
    <form ng-submit="$event.preventDefault()" class="form-horizontal">
      <div ng-if="c.data.fields.length > 0">
        <div ng-repeat="field in c.data.fields" class="m-b-xs" ng-if="field.value">
          <label class="m-n field-label">{{field.label}}</label>
          <span ng-switch="field.type" class="field-span">
            <div ng-switch-when="glide_date_time" title="{{field.display_value}}"><sn-time-ago timestamp="::field.value"></sn-time-ago></div>
            <div ng-switch-default >{{field.display_value}}</div>
          </span>
        </div>
      </div>
      <div ng-if="c.data.isMine && (c.data.state == 'requested')" class="question">
        <button ng-if="data.all_clear" type="button" name="approve" class="btn btn-primary btn-question" ng-disabled="c.approvalInProgress" ng-click="c.action('approved')">${Approve}</button>
        <button ng-if="!data.all_clear" type="button" name="approve" class="btn cc-button-warning btn-question" ng-disabled="c.approvalInProgress" ng-click="c.exceptionAction('approved')">${Approve Exception}</button>
        <div class="spacer"></div>
        <button type="button" name="reject" class="btn btn-default btn-question" ng-disabled="c.approvalInProgress" ng-click="c.action('rejected')">${Reject}</button>
      </div>
    </form>
  </div>  
</div>]]></template>
    </sp_widget>
</record_update>
